Please note that the `betw` and `pos` folders contain screenshots of the images that compose the `betw_anim` and `pos_anim` GIFS, which were created manually using the [Animated GIF Maker](https://ezgif.com/maker).


